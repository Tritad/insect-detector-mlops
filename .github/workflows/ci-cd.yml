name: CI-CD

on:
  push:
    branches:
      - "**"
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        env:
          PYTHONPATH: .
        run: pytest -q

  deploy_hf_spaces:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate deployment secrets
        id: precheck
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE_REPO: ${{ secrets.HF_SPACE_REPO }}
        run: |
          if [ -z "${HF_TOKEN}" ] || [ -z "${HF_SPACE_REPO}" ]; then
            echo "HF_TOKEN or HF_SPACE_REPO is missing. Skipping deploy."
            echo "can_deploy=false" >> "$GITHUB_OUTPUT"
          else
            echo "can_deploy=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install deploy tools
        if: steps.precheck.outputs.can_deploy == 'true'
        run: sudo apt-get update && sudo apt-get install -y rsync curl unzip

      - name: Install Git Xet
        if: steps.precheck.outputs.can_deploy == 'true'
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/huggingface/xet-core/refs/heads/main/git_xet/install.sh | sh
          git xet install
          git xet --version

      - name: Clone Hugging Face Space
        if: steps.precheck.outputs.can_deploy == 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE_REPO: ${{ secrets.HF_SPACE_REPO }}
        run: |
          HF_TOKEN_CLEAN="$(echo "${HF_TOKEN}" | tr -d '[:space:]')"
          HF_SPACE_REPO_CLEAN="$(echo "${HF_SPACE_REPO}" | tr -d '[:space:]' | sed -e 's#^https\?://huggingface\.co/spaces/##' -e 's#^huggingface\.co/spaces/##' -e 's#\.git$##')"
          if ! echo "${HF_SPACE_REPO_CLEAN}" | grep -Eq '^[^/]+/[^/]+$'; then
            echo "Invalid HF_SPACE_REPO format. Expected username/space_name or full Space URL."
            exit 1
          fi
          AUTH_REPO_URL="https://oauth2:${HF_TOKEN_CLEAN}@huggingface.co/spaces/${HF_SPACE_REPO_CLEAN}"
          git clone "${AUTH_REPO_URL}" hf-space

      - name: Sync repository to Space
        if: steps.precheck.outputs.can_deploy == 'true'
        run: |
          rsync -av --delete \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '.venv' \
            --exclude '.pytest_cache' \
            --exclude 'hf-space' \
            --exclude 'model/onnx_v2/model.onnx' \
            ./ hf-space/

      - name: Commit and push
        if: steps.precheck.outputs.can_deploy == 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE_REPO: ${{ secrets.HF_SPACE_REPO }}
        run: |
          cd hf-space
          HF_TOKEN_CLEAN="$(echo "${HF_TOKEN}" | tr -d '[:space:]')"
          HF_SPACE_REPO_CLEAN="$(echo "${HF_SPACE_REPO}" | tr -d '[:space:]' | sed -e 's#^https\?://huggingface\.co/spaces/##' -e 's#^huggingface\.co/spaces/##' -e 's#\.git$##')"
          AUTH_REPO_URL="https://oauth2:${HF_TOKEN_CLEAN}@huggingface.co/spaces/${HF_SPACE_REPO_CLEAN}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "${AUTH_REPO_URL}"
          git pull --rebase origin main || true
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to deploy."
            exit 0
          fi
          git commit -m "chore: auto deploy from GitHub Actions (${GITHUB_SHA})"
          git push origin HEAD:main || (git pull --rebase origin main && git push origin HEAD:main)

  deploy_hf_ui_space:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate UI deployment secrets
        id: precheck_ui
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_UI_SPACE_REPO: ${{ secrets.HF_UI_SPACE_REPO }}
        run: |
          if [ -z "${HF_TOKEN}" ] || [ -z "${HF_UI_SPACE_REPO}" ]; then
            echo "HF_TOKEN or HF_UI_SPACE_REPO is missing. Skipping UI deploy."
            echo "can_deploy=false" >> "$GITHUB_OUTPUT"
          else
            echo "can_deploy=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install deploy tools
        if: steps.precheck_ui.outputs.can_deploy == 'true'
        run: sudo apt-get update && sudo apt-get install -y rsync

      - name: Clone Hugging Face UI Space
        if: steps.precheck_ui.outputs.can_deploy == 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_UI_SPACE_REPO: ${{ secrets.HF_UI_SPACE_REPO }}
        run: |
          HF_TOKEN_CLEAN="$(echo "${HF_TOKEN}" | tr -d '[:space:]')"
          HF_UI_SPACE_REPO_CLEAN="$(echo "${HF_UI_SPACE_REPO}" | tr -d '[:space:]' | sed -e 's#^https\?://huggingface\.co/spaces/##' -e 's#^huggingface\.co/spaces/##' -e 's#\.git$##')"
          if ! echo "${HF_UI_SPACE_REPO_CLEAN}" | grep -Eq '^[^/]+/[^/]+$'; then
            echo "Invalid HF_UI_SPACE_REPO format. Expected username/space_name or full Space URL."
            exit 1
          fi
          AUTH_REPO_URL="https://oauth2:${HF_TOKEN_CLEAN}@huggingface.co/spaces/${HF_UI_SPACE_REPO_CLEAN}"
          git clone "${AUTH_REPO_URL}" hf-ui-space

      - name: Prepare UI Space bundle
        if: steps.precheck_ui.outputs.can_deploy == 'true'
        run: |
          rm -rf hf-ui-build
          mkdir -p hf-ui-build/.streamlit
          cp ui/app.py hf-ui-build/app.py
          cp ui/app.py hf-ui-build/streamlit_app.py
          cp .streamlit/config.toml hf-ui-build/.streamlit/config.toml
          cp requirements-ui-space.txt hf-ui-build/requirements.txt
          cp ui/README-space.md hf-ui-build/README.md

      - name: Sync UI bundle to Space
        if: steps.precheck_ui.outputs.can_deploy == 'true'
        run: |
          cd hf-ui-space
          rm -rf hf-ui-space hf-ui-build
          rsync -av --delete --exclude '.git' ../hf-ui-build/ ./

      - name: Commit and push UI Space
        if: steps.precheck_ui.outputs.can_deploy == 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_UI_SPACE_REPO: ${{ secrets.HF_UI_SPACE_REPO }}
        run: |
          cd hf-ui-space
          HF_TOKEN_CLEAN="$(echo "${HF_TOKEN}" | tr -d '[:space:]')"
          HF_UI_SPACE_REPO_CLEAN="$(echo "${HF_UI_SPACE_REPO}" | tr -d '[:space:]' | sed -e 's#^https\?://huggingface\.co/spaces/##' -e 's#^huggingface\.co/spaces/##' -e 's#\.git$##')"
          AUTH_REPO_URL="https://oauth2:${HF_TOKEN_CLEAN}@huggingface.co/spaces/${HF_UI_SPACE_REPO_CLEAN}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "${AUTH_REPO_URL}"
          git pull --rebase origin main || true
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to deploy for UI Space."
            exit 0
          fi
          git commit -m "chore: auto deploy UI from GitHub Actions (${GITHUB_SHA})"
          git push origin HEAD:main || (git pull --rebase origin main && git push origin HEAD:main)
